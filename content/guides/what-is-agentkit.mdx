---
title: 'What Is AgentKit? The Claude Code Toolkit, Explained'
description: AgentKit (formerly ClaudeKit) is a paid kit of skills, subagents, and hooks that turns Claude Code into a structured plan-build-review-ship workflow. Here is what it actually is and what is in it.
category: Start Here
difficulty: beginner
updated: '2026-07-12'
keywords:
  - what is agentkit
  - what is claudekit
  - agentkit claude code
  - agentkit explained
relatedSkills:
  - plan
  - cook
  - code-review
---

**AgentKit** is a paid toolkit for [Claude Code](https://code.claude.com) — a curated,
maintained set of skills, specialised subagents, and hooks, pre-wired into a
plan → build → review → ship workflow. It was previously called **ClaudeKit**; the name
changed in 2026 (see [the migration note](/guides/claudekit-to-agentkit)).

It does not add anything Claude Code technically withholds — skills, subagents and hooks
are all [free, first-party features](/guides/skills-vs-commands-vs-subagents). What you
are buying is the **curation and the wiring**: a large set of pieces that already fit
together, so you are not authoring and tuning dozens of config files yourself.

## What's actually in it

AgentKit ships as two kits (plus a bundle):

| Kit | For | Contains |
|---|---|---|
| **Engineer** ($99) | Developers | 91 skills, 16 subagents — frontend, backend, database, DevOps, code review, debugging |
| **Marketing** ($99) | Growth teams | 73 skills, 32 subagents — research, SEO, email, CRM |
| **Bundle** ($149) | People who do both | Everything in both |

Both rows are counted from the kits themselves, using `ak kit list-kits` against the CLI
on our own machine — not copied from a marketing page:

```
NAME       VERSION  AGENTS  SKILLS  HOOKS
core       0.1.0        10      54     30
engineer   0.2.0        16      91     17
marketing  0.2.0        32      73      3
```

<Callout type="info" title="We got this wrong before, and here is the correction">
An earlier version of this page said the Engineer kit had **88 skills and 13 agents**, and
told you AgentKit's advertised "17 agents" was inflated. That was wrong. Those numbers came
from the *ClaudeKit-era* kit we had vendored, which the rename superseded. The current kit
ships **91 skills and 16 agents** — so the vendor's figure was roughly right and ours was
stale. We recounted against the live `ak` CLI on 2026-07-13.
</Callout>

This site documents the **Engineer** kit. Its 91 skills fall into categories you can
browse in full on the [skills page](/skills):

- **Utilities (34)** and **Development Tools (26)** — the bulk: planning, cooking
  (implementing), reviewing, testing, debugging, git, scouting a codebase
- **Frontend & Design (12)**, **Backend (3)**, **Database (1)**, **Infra/DevOps (2)**
- **AI/ML (3)**, **Frameworks (4)**, **Multimedia (1)**, **Security (1)**, **Other (4)**

And **16 subagents** — planner, researcher, code-reviewer, debugger, tester, and more —
each running in [its own context window](/guides/skills-vs-commands-vs-subagents) so they
can do heavy work without flooding your main conversation. Three of them are new since the
rename: `advisor`, `explore`, and `kongming` — the last of which runs on the strongest
model available and returns advice, not code, when a design call is genuinely hard.

## The three building blocks

Everything in the kit is one of three Claude Code primitives. If you understand these,
you understand what the kit is made of:

- **Skills** — reusable instructions Claude loads when relevant (`/ak:plan`, `/ak:cook`).
- **Subagents** — workers with a separate context that return a summary.
- **Hooks** — scripts that fire automatically on events (before a command runs, after a
  file is written). AgentKit uses these for guardrails.

We wrote a [full guide to the difference](/guides/skills-vs-commands-vs-subagents), and a
[set of 26 free hooks](/guides/claude-code-hooks-examples) you can use with or without the
kit.

## Is it worth paying for?

That depends entirely on whether you value a working setup over building your own — there
are large free alternatives with *more* agents. We wrote an
[honest worth-it comparison](/guides/agentkit-vs-free-alternatives) that includes the
cases where you should **not** buy it, and a guide to
[which kit to buy](/guides/which-agentkit-kit-to-buy) if you decide to.

## Next steps

- **New to it?** [Install AgentKit](/guides/install-agentkit) — takes a few minutes.
- **Coming from ClaudeKit?** [What changed in the rename](/guides/claudekit-to-agentkit).
- **Weighing the purchase?** [Is it worth it vs the free alternatives](/guides/agentkit-vs-free-alternatives).

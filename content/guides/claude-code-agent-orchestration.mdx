---
title: 'Claude Code Agent Orchestration: Patterns That Actually Work'
description: How to coordinate multiple Claude Code subagents — sequential chaining, parallel fan-out, context isolation, and the file-based handoff that keeps them in sync. With the rules for when parallel helps and when it corrupts.
category: Deep Dives
difficulty: advanced
updated: '2026-07-12'
keywords:
  - claude code agent orchestration
  - claude code multi agent
  - claude code parallel subagents
  - claude code subagent patterns
relatedSkills:
  - plan
  - cook
  - code-review
---

Orchestration is the difference between one agent doing everything in a crowded context
and several agents each doing one thing in their own. Done well, a reviewer subagent
catches bugs the builder wrote, and a research subagent reads fifty files without putting
any of them in your main window. Done badly, parallel agents trample each other's edits.

This is the set of patterns that hold up, why each works, and the rules for not corrupting
your own work.

## The one thing orchestration buys you: context

A [subagent](/guides/skills-vs-commands-vs-subagents) runs in its own context window and
returns only a summary. That single property is what every pattern below exploits:

- The heavy, disposable work (searching, reading logs, exploring) happens *elsewhere*.
- A fresh context has no accumulated confusion — a reviewer that never saw the code being
  written judges it on its merits.
- Different agents can have different tools and even different models.

## Pattern 1 — Sequential chaining

Use when each step depends on the last. Plan → build → review → ship:

```text
plan          → writes a plan to disk
  └─ build     → reads the plan, implements it
      └─ review → reads the diff, finds problems
          └─ ship → commits once review passes
```

Each stage is a fresh context that picks up where the last left off — through a
**file on disk**, not through conversation memory. The planner writes `plan.md`; the
builder reads it. This is why the handoff survives even when the conversation is
compacted or restarted.

**When it's worth it:** any task big enough that doing plan+build+review in one context
means the review step is judging code it just wrote, with all the motivated reasoning that
implies. Separating them removes that bias.

## Pattern 2 — Parallel fan-out

Use when the work splits into independent pieces. Spawn several subagents at once, each
on a different slice, then merge:

```text
             ┌─ researcher: database options
controller ──┼─ researcher: auth approaches
             └─ researcher: UI frameworks
             (all run at once, each returns a summary)
```

Wall-clock time is the *slowest single agent*, not the sum. Three researchers finish in
the time of one. The controller reads three summaries instead of doing three
investigations in one context.

**This page was built this way** — a planning pass spawned two research subagents in
parallel to map the SERP landscape, and later a reviewer and a red-team subagent
independently attacked the plan. The red-team caught a wrong number the plan was built on.
That is orchestration earning its keep: an independent context has no stake in the
conclusion.

## Pattern 3 — Adversarial verification

Use when being *confident* matters more than being fast. Spawn a subagent whose only job
is to **refute** a finding or a plan:

```text
finding ──→ verifier 1 (try to prove it wrong)
       └──→ verifier 2 (try to prove it wrong)
       └──→ verifier 3 (try to prove it wrong)
kill the finding if a majority refute it
```

A single agent that produced a plausible-but-wrong answer will defend it. Three fresh
agents told to attack it will not. This is how you stop confident-sounding mistakes from
surviving.

## Pattern 4 — Model escalation

The three patterns above all move work *sideways*. This one moves it **up**.

Subagents can run on different models. Most people use that to save money — send the
disposable reading to a cheap tier, keep the thinking on an expensive one. The move almost
nobody makes is the reverse: **let a cheap agent that is stuck call a stronger one, by
itself, without a human noticing and switching the session model.**

```text
  tier     agent          may do
  ─────────────────────────────────────────────
  cheap    explorer       read only
  mid      builder,       read + write
           tester
  top      advisor        advise only — cannot write

  The tester hits a wall: repeated failures, or a design fork above its pay grade.

      tester ──── spawns ───▶ advisor          (up a tier)
         ▲                       │
         └──── counsel ──────────┘             (one reply, no interview)

  The tester still owns the work. Only the *thinking* was escalated.
```

**Why the escalated agent must be advisory-only.** This is the part that is easy to get
wrong, and it is the whole safety property. If the stronger agent could write code, you
have just handed your most capable model unsupervised write access to fix a problem it
only half understands — it has the question, not your context. Restricting it to *advice*
is not a limitation. It is what makes it safe to escalate at all.

**And it must not ask questions.** An advisor that stops to interview defeats the point:
the caller is a *machine*, mid-task, with nobody watching. It has to pick the most
reasonable assumption from the evidence, proceed, and record what it assumed and how
confident it was. A stalled escalation is a hung agent.

**Do it free:** an advisory-only agent is a file. `.claude/agents/advisor.md`, with the
strongest model you have access to, and a prompt that forbids editing:

```md
---
name: advisor
description: Counsel for a stuck agent. Advisory only — never writes code.
tools: Read, Grep, Glob, WebSearch
model: opus
---

You are consulted by other agents that are stuck. Read the code before opining.
Return: the recommendation, what to avoid, the alternatives with honest costs, and
every assumption you made in place of a question, with a confidence level.
You never edit files. Your reply is the deliverable — there is no second turn.
```

Note the `tools:` line: no `Write`, no `Edit`, no `Bash`. **A system prompt that says
"advisory only" is a promise; a tools list that omits `Write` is a fence.** Only one of
those survives an agent that decides it would be helpful to just fix the thing.

Then tell your agents, in your rules file, that being stuck is a reason to escalate rather
than thrash. That is the entire pattern, and it costs nothing.

## The rules — when parallel corrupts

Parallelism is not free, and used wrong it destroys work. The hard rules:

| Rule | Why |
|---|---|
| **Clear file ownership.** Two agents must never edit the same file at once. | Concurrent edits clobber each other; the last writer wins and the other's work vanishes. |
| **No shared mutable state.** Not the same config, not the same migration sequence, not the same generated artifact. | The same failure mode as file edits, harder to see. |
| **Integration points known up front.** | If agent A's output feeds agent B, that's *sequential*, not parallel — don't fake it. |
| **Controller keeps the decisions.** Merges, approvals, and conflict resolution stay in one place. | Distributed decision-making across agents produces incoherent results. |

The test: if you can't say in one sentence which files each agent owns, you are not ready
to run them in parallel. Do it sequentially instead.

## Context isolation — the discipline that makes it work

When you hand a task to a subagent, do **not** hand it your whole conversation. Give it:

- the specific task,
- the exact files to read (not "look around the repo"),
- the files it may modify,
- what "done" looks like,
- where to write its report.

A subagent with a clean, scoped brief does better work than one drowning in your history —
the whole point was a fresh context, so don't refill it.

## Where this stops: one session, many agents

Everything above happens inside **one** Claude Code session. The controller spawns
subagents, reads their summaries, and keeps the decisions.

There is a further step — several *sessions* working the same repo as teammates, handing
off through files rather than through a controller's context. AgentKit ships it as
`/ak:team`. **Be honest about the size of this:** it is a niche feature. It earns its place
when work genuinely runs for longer than one session survives, and it costs you the one
thing single-session orchestration gives you for free — a single place where merges and
approvals happen. Most work that feels like it needs a team needs a clearer plan instead.

If you do reach for it, the rules from the parallel section apply twice as hard: file
ownership must be explicit, and one session has to own the merge.

## How AgentKit does this

[AgentKit](https://agentkit.best)'s value in this area is that its 16 subagents come
pre-scoped: a `code-reviewer` with read-only tools, a `researcher`, a `planner`, a
`debugger`, each already configured with the right tool access and a focused system
prompt — plus `kongming`, an advisory-only agent on the strongest tier, which is the
escalation target in Pattern 4 already wired up.

You *can* build all of these yourself — the patterns above are all free, and the
twenty-line advisor above is the whole of Pattern 4 — but "already built and tuned to hand
off to each other" is the thing the kit sells. Whether that's worth $99 depends on how much
of this you'd otherwise assemble; we wrote an
[honest comparison](/guides/agentkit-vs-free-alternatives).

**One caveat worth printing**, because the kit does not: the escalation to the strongest
tier is guaranteed only on Claude Code. On other runtimes that tier is simply absent, and
the agent runs on whatever the host defaults to — still following its prompt, but not
actually stronger than its caller. An escalation that does not escalate is worth knowing
about before you build a workflow on it.

## Related

- [Skills vs commands vs subagents](/guides/skills-vs-commands-vs-subagents) — the primitive underneath all of this
- [Claude Code workflow patterns](/guides/claude-code-workflow-patterns) — single-agent workflows, for when orchestration is overkill
